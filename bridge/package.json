{
  "name": "okx-backfill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-time backfill of OKX's full ledger since 2021 into Supabase (crypto_bills). Rolling 3-month history is handled by api/okx/pnl-history.mjs + api/cron/okx-sync.mjs.",
  "bin": { "okx-backfill": "./okx-backfill.mjs" },
  "scripts": { "backfill": "node okx-backfill.mjs" },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "adm-zip": "^0.5.16"
  },
  "engines": { "node": ">=18" }
}
